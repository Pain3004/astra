pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
                bat 'php --version'
                bat 'composer update'
                bat 'php artisan serve'
            }
        }
        stage("Unit test") {
            steps {
                bat 'php artisan test'
            }
        }
    }
}
